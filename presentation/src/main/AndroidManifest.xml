<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- The camera app cannot be handed a file:// path, so a shot is written
             through this provider into the same directory the app keeps its own
             word pictures in. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.images"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/word_image_paths" />
        </provider>
    </application>

</manifest>
